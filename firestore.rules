
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /vendor-applications/{applicationId} {
      allow create: if true;
      allow read, update, delete: if request.auth != null;
    }
  }
}
